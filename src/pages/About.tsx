import { DrawerComponent } from "../components/Drawer";
const content = "";
const context = "About";
function About() {
  return <DrawerComponent context={context} content={content} />;
}

export default About;
